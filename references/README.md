# References

This folder is reserved for upstream reference scripts that define the intended canonical shape of the engine.

Expected inputs:

- `0313_DailyMap_Formal_IND_V5`
  - role: indicator-side structural reference
- `1150412106`
  - role: trading-side safety-layer reference

Recommended next action:

- place sanitized copies of those files here
- document their provenance
- map each safety or structure rule back into `docs/ENGINE_STANDARD.md`

Until the reference scripts are imported, the templates in `templates/xs/` should be treated as safe bootstrap scaffolding rather than final production outputs.
